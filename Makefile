# Test Service - simple dummy daemon for HGW-Doctor validation
# This is the project Makefile, not the OpenWrt package Makefile
# The OpenWrt package Makefile is in test-service/Makefile

.PHONY: all clean

all:
	@echo "Nothing to build - this is a shell script service"

clean:
	@echo "Nothing to clean"
